version: '3'
services:
  relayer:
    build: .
    env_file:
      - ./heiswap.env
    expose:
      - "3000"
  nginx:
    image: valian/docker-nginx-auto-ssl
    restart: on-failure
    ports:
      - 80:80
      - 443:443
    volumes:
      - ssl_data:/etc/resty-auto-ssl
    env_file:
      - ./heiswap.env

volumes:
  ssl_data: